checks:
  - name: build-size-watcher
    options:
      files:
        - path: 'packages/*/lib/esm.min.js'
  - name: type-coverage-watcher
    options:
      ignoreCatch: true
      ignoreFiles:
        - '*.d.ts'
      strict: true
